import React from 'react';

const heroPage = {
    height: "100%",
    background: "blue",
}

const Hero = () => {
    return (
        <div>
            Here is the Hero Container
        </div>
    );
}

export default Hero;
